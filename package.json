{
  "name": "agent-skill-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for AI Agent Skills visualization.",
  "scripts": {
    "start": "serve public -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}